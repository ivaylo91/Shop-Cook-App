<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+. The lists (the database) are backed up and moved to a new
     phone; the sign-in session is not, so a restored or copied backup never
     carries a working login. Signing in again on the new phone is enough. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="FlutterSharedPreferences.xml" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="FlutterSharedPreferences.xml" />
    </device-transfer>
</data-extraction-rules>
